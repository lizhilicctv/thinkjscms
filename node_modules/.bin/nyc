#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_nyc@7.1.0@nyc/bin/nyc.js" "$@"
  ret=$?
else 
  node  "$basedir/../_nyc@7.1.0@nyc/bin/nyc.js" "$@"
  ret=$?
fi
exit $ret
